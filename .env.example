PORT=3000

# Postgres connection string for the Supabase database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# Supabase JWKS endpoint for verifying ES256 (asymmetric) Bearer tokens.
# Found under Supabase → Settings → API → JWT Signing Keys (Discovery URL).
SUPABASE_JWKS_URL=https://<project-ref>.supabase.co/auth/v1/.well-known/jwks.json

# Comma-separated Supabase user ids allowed to use this API. A valid Supabase
# token is NOT sufficient on its own: nothing here is scoped per user, so any
# account in the project could otherwise read and write everything.
# Find yours under Supabase → Authentication → Users.
ALLOWED_USER_IDS=00000000-0000-0000-0000-000000000000

# Optional: comma-separated list of allowed CORS origins.
# Leave unset to reflect any origin (fine for local dev / Bearer-token auth).
# CORS_ORIGIN=https://app.example.com,https://staging.example.com
